name: vite-VPS CI/CD

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"

      # Copia el package.json al directorio de trabajo
      - name: Copy package.json
        run: cp package.json .
      # Ejecuta npm install para instalar las dependencias
      - run: npm ci
      - run: npm run build --if-present
      # teste Eliminado temporalmente
      #- run: npm test
      - name: Debug Information
        run: |
          echo "Node Version: $(node -v)"
          echo "NPM Version: $(npm -v)"
          echo "Current Directory: $(pwd)"
          echo "Directory Listing:"
          ls -la

  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Install Dependencies
        run: npm install
        working-directory: /home/wallydev/web/wallydev.dev/public_html

      - name: Copy files via SSH
        uses: appleboy/scp-action@v0.1.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "dist/"
          target: "/home/web/wallydev.dev/public_html"

      - name: Execute remote commands via SSH
        uses: appleboy/ssh-action@v0.1.1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/web/wallydev.dev/public_html
            # Reinicia tu aplicación si es necesario
